# Remaining conversation acceptance

The package's MCP protocol and real Claude Code plugin connection can be verified without model usage. The next gate is a conversation in an authorized client using the final release. Claude Desktop reported a message cap until September 21 at 01:00 during rc.2 preparation; no credits were purchased.

Use a fresh test directory and retain the final client conversation and server receipts privately. No user data or existing jobs are fixtures. Ask the model to:

1. Discover Commander's exact local device and runtime capabilities.
2. Create one small fixture file using an explicit call ID, then repeat the same intent and identity. Verify one effect.
3. Launch four noninteractive workers with pipe transport, separate directories, stable IDs, and declared output hashes. Collect all outputs and report each exit code.
4. Reconnect the client, collect the same batch, and establish that no worker ran again.
5. Run one worker that exits 7 and correctly report failure even though the observation tool succeeded.
6. Report the difference between observed artifact success and an arbitrary user goal being verified.

Use the same model and prompts against RDC when measuring model-mediated completion rates. The execution-route reports do not substitute for this test. Do not reset usage, enable paid credits, change providers, or provision a public service without the user's corresponding instruction.
